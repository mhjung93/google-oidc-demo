# CLAUDE.md

## 기본 원칙
- 모든 응답은 반드시 한글로 작성한다.
- 매 동작(파일 수정, 명령 실행, git 작업 등)마다 먼저 계획을 설명하고, 사용자의 명시적 승인을 받은 뒤 진행한다.
- 사용자가 요청하지 않은 작업은 임의로 수행하지 않는다. 작업 범위는 사용자가 요청한 내용으로 제한한다.
- 불확실한 부분이 있으면 추측해서 진행하지 말고 사용자에게 확인한다.

## 파일 수정 규칙
- 파일을 수정하기 전에 변경 대상 파일과 변경 이유를 먼저 설명하고 승인을 받는다.
- 최소 변경 원칙을 따르고, 요청과 무관한 파일은 수정하지 않는다.
- 기존 코드 스타일, 폴더 구조, 네이밍 규칙을 최대한 유지한다.
- 공개 API, 함수 시그니처, 데이터 구조는 사용자가 명시적으로 요청하지 않는 한 변경하지 않는다.
- 대규모 리팩토링, 새 의존성 추가는 사용자의 명시적 승인 없이 수행하지 않는다.
- 자동 포맷팅으로 무관한 파일 전체가 변경되지 않도록 주의한다.

## 보안 및 민감정보
- `.env`, `.env.local`, `credentials.json`, `secrets.json`, private key, API key, 인증 토큰 파일(`.id_token.txt`, `auditor_keys.json`, `bip32_out.json` 등 포함)은 읽거나 출력하거나 수정하지 않는다.
- 민감정보가 포함될 가능성이 있는 파일을 열어야 할 경우, 먼저 사용자에게 확인한다.
- 로그, 오류 메시지, 설정 파일에 포함된 비밀값은 응답에 그대로 출력하지 않는다.
- API key, secret, token, password는 절대 코드에 하드코딩하지 않는다.

## 코드 작성 규칙
- 명확하고 단순한 코드를 우선하고, 과도하게 복잡한 추상화는 피한다.
- 기존 코드의 의도를 보존하며, 주석은 명확하지 않은 로직을 설명할 때만 작성한다.
- 에러 처리는 명시적으로 작성하고, 테스트 가능한 구조를 우선한다.
- 성능 최적화는 사용자가 요청하거나 명확한 병목이 확인된 경우에만 수행한다.

## 테스트 및 검증
- 코드 수정 후 가장 작은 범위의 관련 테스트부터 실행하며, 실행 전 사용자에게 먼저 확인한다.
- 전체 테스트가 오래 걸릴 가능성이 있으면 smoke test/단위 테스트 수준의 검증을 먼저 제안한다.
- 테스트를 실행하지 못했다면 이유와 미검증 위험을 명확히 보고한다.
- 테스트 결과는 성공/실패 여부와 핵심 오류만 간결하게 정리한다.
- `package.json`의 `test` 스크립트는 placeholder(`exit 1`)이다. 실제 테스트는 `test_mode2_integration.js`, `test_ps.js` 같은 개별 node 스크립트를 직접 실행하는 방식이므로, `npm test`가 아니라 해당 스크립트를 사용한다.

## Git 작업 규칙
- git commit, branch 생성, reset, rebase, merge, push는 사용자의 명시적 승인 없이 수행하지 않는다.
- 변경 후에는 수정된 파일 목록과 주요 변경 내용을 요약한다.
- commit 메시지는 제안만 하고, 사용자가 요청하지 않는 한 실제 commit은 수행하지 않는다.
- 최상위 `.git`은 현재 초기화되지 않은 상태일 수 있다(빈 디렉터리). `git init`을 포함해 저장소 상태를 바꾸는 작업은 실행 전 사용자에게 먼저 알리고 승인을 받는다.
- `sui-repo/`는 자체 `.git`을 가진 별도의 중첩 저장소다. 최상위에서 git 명령(특히 `git add -A` 등 광범위한 스테이징)을 실행할 때 이 디렉터리가 의도치 않게 포함되지 않도록 주의한다.

## 연구 코드 및 재현성 규칙
- raw data 파일, 기존 결과 파일은 사용자의 명시적 승인 없이 덮어쓰지 않는다.
- random seed, sampling 조건, 실험 파라미터는 임의로 변경하지 않는다.
- 재현성에 영향을 줄 수 있는 변경은 반드시 별도로 보고한다.
- 하드코딩보다 설정 파일 또는 명령행 인자를 우선한다.
- 분석 결과를 바꾸는 수정과 코드 정리 목적의 수정은 명확히 구분한다.

## 문서 작성 규칙
- README, 보고서, 주석 문서를 수정할 때는 기존 용어와 문체를 유지한다.
- 기본 응답은 항상 한글로 작성하되, 사용자가 영어를 요청하면 영어로 작성한다.
- 과장된 표현을 피하고, 근거가 불충분한 주장은 명확히 표시한다.

## 프로젝트 구조 및 특이사항 (google-oidc-demo)
- 이 프로젝트는 Google OIDC + zk-SNARK 기반 SSO 데모다. Mode 1(Google ID Token을 지갑 키에 바인딩)과 Mode 2(Custom IdP + PS 서명 + zk 증명 기반 SSO, `MODE2_FLOW.md` 참고) 두 흐름이 공존한다.
- 작업 요청이 특정 Mode에 대한 것인지 먼저 확인한다. Mode 2 작업 시 `MODE2_FLOW.md`의 "Files Usually Not Needed for Mode 2" 목록(`make_proof.js`, `derive_bip32.js`, `circuits/bind_key_to_idtoken.circom`, `run_all.sh`, `snap/src/tx_type2_accesslist.js`, `contracts/Greeter.sol`)에 있는 파일은 명시적 요청 없이 수정하지 않는다. 마찬가지로 Mode 1 작업 시 Mode 2 전용 파일을 임의로 건드리지 않는다.
- `build/`, `artifacts/`, `*.ptau`(최대 2.4GB), `*.zkey`(최대 382MB), `*.r1cs`, `*.wtns`는 모두 생성된 산출물이다. `npm run zk:ptau`, `zk:key`, `zk:compile` 등 이를 재생성하는 명령은 시간이 오래 걸리고 기존 증명/키를 무효화할 수 있으므로, 사용자의 명시적 요청 없이 실행하지 않는다.
- `MODE2_FLOW.md`에 문서화된 대로 이 코드는 데모/프로토타입 단계다(예: RP-side ZKP 실패 처리가 데모 호환성을 위해 permissive함, `verifyPS_Hybrid()`가 완전한 프로덕션 검증이 아님, `custom_idp.js`의 `testuser`/`password123` 같은 하드코딩 데모 계정). 이런 부분을 임의로 "버그"나 "취약점"으로 판단해 고치지 않고, 의도된 데모 동작인지 먼저 사용자에게 확인한다.
- `backup_current_project/`, `client_backup.js`, `client_260111.js` 등은 과거 스냅샷이다. 활성 파일(`client.js`, `server.js`, `custom_idp.js`)과 혼동하지 않고, 사용자가 명시하지 않는 한 백업 파일은 수정하지 않는다.

## 작업 진행 방식
1. 현재 상황을 분석한다.
2. 변경 계획을 제시하고 사용자의 승인을 받는다.
3. 승인 후 작업을 수행한다.
4. 변경사항과 테스트/검증 결과를 요약하고, 남은 위험요소를 제시한다.
