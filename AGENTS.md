# AGENTS.md

## 기본 원칙

* 모든 응답은 반드시 한글로 작성한다.
* 매 동작마다 항상 사용자에게 먼저 물어보고, 사용자의 명시적 승인을 받은 뒤 진행한다.
* 파일을 수정하기 전에 반드시 수정 계획을 먼저 설명한다.
* 명령어를 실행하기 전에 반드시 어떤 명령을 왜 실행하는지 설명하고 사용자에게 확인받는다.
* 사용자가 요청하지 않은 작업은 임의로 수행하지 않는다.
* 작업 범위는 사용자가 요청한 내용으로 제한한다.
* 불확실한 부분이 있으면 추측해서 진행하지 말고 사용자에게 확인한다.

## 파일 수정 규칙

* 파일을 수정하기 전에 변경 대상 파일과 변경 이유를 먼저 설명한다.
* 최소 변경 원칙을 따른다.
* 요청과 무관한 파일은 수정하지 않는다.
* 기존 코드 스타일, 폴더 구조, 네이밍 규칙을 최대한 유지한다.
* 공개 API, 함수 시그니처, 데이터 구조는 사용자가 명시적으로 요청하지 않는 한 변경하지 않는다.
* 대규모 리팩토링은 사용자의 명시적 승인 없이 수행하지 않는다.
* 새 의존성은 사용자의 명시적 승인 없이 추가하지 않는다.
* 자동 포맷팅으로 무관한 파일 전체가 변경되지 않도록 주의한다.

## 보안 및 민감정보

* `.env`, `.env.local`, `credentials.json`, `secrets.json`, private key, API key, 인증 토큰 파일은 읽거나 출력하거나 수정하지 않는다.
* 민감정보가 포함될 가능성이 있는 파일을 열어야 할 경우, 먼저 사용자에게 확인한다.
* 로그, 오류 메시지, 설정 파일에 포함된 비밀값은 응답에 그대로 출력하지 않는다.
* API key, secret, token, password는 절대 코드에 하드코딩하지 않는다.

## 코드 작성 규칙

* 명확하고 단순한 코드를 우선한다.
* 과도하게 복잡한 추상화는 피한다.
* 기존 코드의 의도를 보존한다.
* 주석은 필요한 경우에만 작성하고, 명확하지 않은 로직을 설명하는 용도로 사용한다.
* 에러 처리는 가능한 한 명시적으로 작성한다.
* 테스트 가능한 구조를 우선한다.
* 성능 최적화는 사용자가 요청하거나 명확한 병목이 확인된 경우에만 수행한다.

## 테스트 및 검증

* 코드 수정 후에는 가능한 가장 작은 범위의 관련 테스트부터 실행한다.
* 테스트 명령을 실행하기 전 사용자에게 먼저 확인한다.
* 전체 테스트가 오래 걸릴 가능성이 있으면, 먼저 smoke test 또는 단위 테스트 수준의 검증을 제안한다.
* 테스트를 실행하지 못한 경우, 그 이유와 미검증 위험을 명확히 보고한다.
* 테스트 결과는 성공/실패 여부와 핵심 오류만 간결하게 정리한다.

## Git 작업 규칙

* Git commit, branch 생성, reset, rebase, merge, push는 사용자의 명시적 승인 없이 수행하지 않는다.
* 변경 후에는 수정된 파일 목록과 주요 변경 내용을 요약한다.
* 최종 응답에는 변경 파일, 변경 내용, 테스트 결과, 남은 위험요소를 포함한다.
* 사용자가 요청하지 않는 한 commit 메시지는 제안만 하고 실제 commit은 수행하지 않는다.

## 연구 코드 및 재현성 규칙

* raw data 파일은 절대 수정하거나 덮어쓰지 않는다.
* 기존 결과 파일은 사용자의 명시적 승인 없이 덮어쓰지 않는다.
* random seed, sampling 조건, 실험 파라미터는 임의로 변경하지 않는다.
* 재현성에 영향을 줄 수 있는 변경은 반드시 별도로 보고한다.
* 하드코딩보다 설정 파일 또는 명령행 인자를 우선한다.
* 분석 결과를 바꾸는 수정과 코드 정리 목적의 수정은 명확히 구분한다.

## 문서 작성 규칙

* README, 논문 초안, 보고서, 주석 문서를 수정할 때는 기존 용어와 문체를 유지한다.
* 기술 용어는 가능하면 일관되게 사용한다.
* 사용자가 한글을 요청하면 한글로, 영어를 요청하면 영어로 작성하되, 기본 응답은 항상 한글로 작성한다.
* 논문 또는 연구 문서에서는 과장된 표현을 피하고, 근거가 불충분한 주장은 명확히 표시한다.

## 작업 진행 방식

* 먼저 현재 상황을 분석한다.
* 그다음 변경 계획을 제시한다.
* 사용자의 승인을 받은 뒤 작업을 수행한다.
* 작업 후 변경사항을 요약한다.
* 테스트 또는 검증 결과를 보고한다.
* 추가로 확인해야 할 위험요소가 있으면 명확히 제시한다.

